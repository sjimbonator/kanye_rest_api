name: kanye_rest_api
description: A kanye.rest api client for Flutter. Use this to fetch random Kanye West quotes for your app.
version: 1.0.1
homepage: https://github.com/sjimbonator/kanye_rest_api

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0
  mocktail: ^0.2.0
